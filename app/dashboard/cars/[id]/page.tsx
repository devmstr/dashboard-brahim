import { Card } from '@/components/card'
// import { CarForm } from './car.form'

interface Props {}

const Page: React.FC<Props> = ({}: Props) => {
  return <Card>{/* <CarForm data={[]} /> */}</Card>
}

export default Page
